# Claude Code — instructions

Les instructions pour ce repo vivent dans [`AGENTS.md`](AGENTS.md), pas ici — lis ce fichier en premier avant toute modification.

Ce fichier existe uniquement parce que Claude Code cherche spécifiquement un `CLAUDE.md` à la racine. Tout le reste (stack, règles critiques, checklist "ajouter une page", déploiement, portée des actions) est dans `AGENTS.md` pour rester utilisable par n'importe quel agent IA, pas seulement Claude Code.
